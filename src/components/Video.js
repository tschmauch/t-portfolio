import React, { Component } from 'react';
import {Route, Link} from 'react-router-dom';

class Video extends Component {
    render() {
      return (
        <div className='video row mainRow'>
         
        </div>
      );
    }
  }

export default Video;